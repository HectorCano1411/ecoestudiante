# Auth0 Configuration

## Variables de Entorno Requeridas

Crea un archivo `.env.local` en `ecoestudiante-web/` con las siguientes variables:

```bash
# Auth0 Configuration
AUTH0_SECRET=CHANGEME_USE_openssl_rand_hex_32_to_generate
AUTH0_BASE_URL=http://localhost:3000
AUTH0_ISSUER_BASE_URL=https://<YOUR_AUTH0_DOMAIN>
AUTH0_CLIENT_ID=<YOUR_CLIENT_ID>
AUTH0_CLIENT_SECRET=<YOUR_CLIENT_SECRET>
AUTH0_AUDIENCE=<YOUR_API_IDENTIFIER>

# Gateway URL
GATEWAY_URL=http://localhost:8080
```

## Generar AUTH0_SECRET

```bash
openssl rand -hex 32
```

## Configuración de Cookies

En producción, las cookies se configuran automáticamente como:
- Secure: true (solo HTTPS)
- HttpOnly: true
- SameSite: Lax

## Flujo Authorization Code + PKCE

El SDK de Auth0 usa PKCE por defecto para aplicaciones SPA, lo cual es más seguro que el flujo tradicional.

